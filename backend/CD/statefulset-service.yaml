apiVersion: v1
kind: Service

metadata:
  name: db-task-manager
  namespace: dev

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: db-task-manager

  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP